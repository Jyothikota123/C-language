#include<stdio.h>
int main()
{
 int x[100],i,n;
 printf("enter the size of an array:");
 scanf("%d",&n);
 for(i=0;i<n;i++)
 {
  printf("enter an element:");
  scanf("%d",&x[i]);
 }
 for(i=0;i<n;i++)
 {
  printf("%d\n",x[i]);
 }
}
