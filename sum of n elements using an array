#include<stdio.h>
int main()
{
 int x[100],i,n,sum=0;
 printf("enter the size of an array:");
 scanf("%d",&n);
 for(i=0;i<n;i++)
 {
  printf("enter an element:");
  scanf("%d",&x[i]);
 }
 for(i=0;i<n;i++)
 {
 sum=sum+x[i];
 }
 printf("sum of elements is %d",sum);
}
